pipeline {

    agent any
    stages {
        stage ('Deploy') {
            steps {
                script {
                    sh "git branch"
                    echo "Deploying container...."
                    sh "docker compose up"
                }
            }
        }
    }
}